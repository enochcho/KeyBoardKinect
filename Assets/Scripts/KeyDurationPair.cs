using System.Collections;
using System.Collections.Generic;
using UnityEngine;

// Currently not in use; if we want to pair notes with durations for a 2D array
public class KeyDurationPair : MonoBehaviour
{
    public Key keyToPlay;
    public float duration;
    // Start is called before the first frame update
    void Start()
    {
        
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
